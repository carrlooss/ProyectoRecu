package com.example.proyectorecu

// Interfaz que define un contrato para manejar clics en imágenes del menú
interface OnFragmentActionListener {

    //Método que se ejecutará cuando se haga clic en una imagen del menú.
    public fun onClickImagenMenu(btn: Int)
}