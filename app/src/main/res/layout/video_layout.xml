<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp"
    android:background="#E0F7FA">

    <!-- Layout contenedor con fondo y bordes redondeados -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:layout_marginBottom="30dp"
        android:background="#FFFFFF"
        android:layout_gravity="center"
        android:padding="8dp"
        android:elevation="6dp"
        android:layout_margin="16dp"
        android:backgroundTint="#FFFFFF"
        android:clipChildren="true"
        android:clipToPadding="true"
        android:radius="16dp">

        <VideoView
            android:id="@+id/videoView"
            android:layout_width="match_parent"
            android:layout_height="300dp"
            android:layout_centerInParent="true"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="8dp" />

        <!-- Agregamos un control de play/pause superpuesto para una mejor experiencia -->
        <ImageView
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:layout_centerInParent="true"
            android:layout_marginTop="-30dp"
            android:contentDescription="Play Button"
            app:tint="#FFFFFF" />

    </RelativeLayout>

    <!-- Añadimos un botón estilizado para más interactividad -->
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="Reproducir Video"
        android:backgroundTint="#00796B"
        android:textColor="#FFFFFF"
        android:padding="12dp"
        android:layout_gravity="center"/>

</LinearLayout>